# OpenAPI Contracts

This directory contains versioned OpenAPI 3.1 source documents. The current contract foundation formalises only the health and readiness routes that exist in the backend and AI-platform scaffolds.

Add endpoints only with an approved vertical slice. Keep operation IDs stable and reference shared schemas instead of copying wire types.
