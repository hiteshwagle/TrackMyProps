# Shared Contracts

This directory is reserved for versioned TrackMyProps API, event, and JSON Schema contracts.

Phase 0 defines directory ownership only. No domain contract or generated client exists yet.

