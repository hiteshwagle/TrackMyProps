# Contract Examples

This directory contains minimal synthetic examples mapped to executable schemas by the contract test suite. Examples contain no real user data, production identifiers, provider payloads, or credentials.
